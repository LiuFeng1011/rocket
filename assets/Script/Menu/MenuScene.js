//游戏菜单界面
var BaseScene = require("../Base/BaseScene");
cc.Class({
    extends: BaseScene,

    properties: {
        
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    init () {
        this.addLayer("menulayer",0);
    },

    // update (dt) {},
});
