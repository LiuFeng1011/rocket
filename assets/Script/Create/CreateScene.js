var BaseScene = require("../Base/BaseScene");

cc.Class({
    extends: BaseScene,

    properties: {
        
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    init () {
        this.addLayer("createLayer",0);

    },

    // update (dt) {},

});
