var BaseScene = require("../Base/BaseScene");

cc.Class({
    extends: BaseScene,

    properties: {
        
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    init () {

        this.addLayer("gameLayer",0);
    },

    // update (dt) {},
});
